{% extends 'base.html.twig' %}

{% block body %}
    <div class="container col-md-8 text-center mt-3">
        <div class="row">
            <div class="col-6">
                <table class="table table-bordered">

                    <tr> 
                        <th>ID</th>
                        <td>{{classroom.id}}
                    </tr>
                    <tr>
                        <th>Name</th>
                        <td>{{classroom.Class_name}} </td>
                    </tr>
                     <tr> 
                        <th>Quantity</th>
                        <td>{{classroom.Class_quantity}}</td>
                    </tr>
                    <tr>
                        <th> Description </th>
                        <td>{{classroom.Class_description}}</td>
                    </tr>
                </table>
            </div>
        </div>
    </div>
{% endblock %}